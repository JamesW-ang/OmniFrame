<UserControl x:Class="OmniFrame.Wpf.Views.BlockCutMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:OmniFrame.Wpf.ViewModels">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>

        <!-- ═══ 顶栏 ═══ -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" CornerRadius="4" Margin="0,0,0,4">
            <Grid Margin="12,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TimeText}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                    <Border Width="1" Background="{StaticResource BorderBrush}" Margin="12,4"/>
                    <TextBlock Text="{Binding UphText}" Foreground="{StaticResource AccentBrush}" FontSize="13" FontWeight="Bold"/>
                    <Border Width="1" Background="{StaticResource BorderBrush}" Margin="12,4"/>
                    <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextBrush}" FontSize="13"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <CheckBox Content="空跑" IsChecked="{Binding IsEmptyTest}" Foreground="{StaticResource TextBrush}" Margin="0,0,12,0"/>
                    <Button Content="▶ 启动" Width="80" Height="28" Background="{StaticResource RunBrush}" Foreground="White" Cursor="Hand" Margin="4,0" Command="{Binding StartCommand}"/>
                    <Button Content="⏸ 暂停" Width="80" Height="28" Background="{StaticResource WarnBrush}" Foreground="White" Cursor="Hand" Margin="4,0" Command="{Binding PauseCommand}"/>
                    <Button Content="⏹ 停止" Width="80" Height="28" Background="{StaticResource ErrorBrush}" Foreground="White" Cursor="Hand" Margin="4,0" Command="{Binding StopCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══ 中间：左侧相机+工站, 右侧工具栏 ═══ -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- 左：相机 + 工站状态 -->
            <Grid Grid.Column="0" Margin="0,0,4,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 4 路相机 -->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource SurfaceDarkBrush}" Margin="2" CornerRadius="4" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <TextBlock Text="相机1 — 底板右侧" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/>
                    </Border>
                    <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource SurfaceDarkBrush}" Margin="2" CornerRadius="4">
                        <TextBlock Text="相机2 — 底板左侧" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/>
                    </Border>
                    <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource SurfaceDarkBrush}" Margin="2" CornerRadius="4">
                        <TextBlock Text="相机3 — 片源" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/>
                    </Border>
                    <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource SurfaceDarkBrush}" Margin="2" CornerRadius="4">
                        <TextBlock Text="相机4 — 辅助" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/>
                    </Border>
                </Grid>

                <!-- 工站状态 + 报警列表 -->
                <Grid Grid.Row="1" Margin="0,4,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <!-- 工站仪表板 -->
                    <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="10" Margin="0,0,2,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <TextBlock Text="工站状态" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding Stations}">
                                <ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="6" Height="6" Margin="0,0,6,0"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="{StaticResource IdleBrush}"/><Style.Triggers><DataTrigger Binding="{Binding IsRunning}" Value="True"><Setter Property="Fill" Value="{StaticResource RunBrush}"/></DataTrigger><DataTrigger Binding="{Binding IsPaused}" Value="True"><Setter Property="Fill" Value="{StaticResource WarnBrush}"/></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="{StaticResource TextBrush}" FontSize="11"/>
                                    <TextBlock Grid.Column="2" Text="{Binding StatusText}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/></Grid></DataTemplate></ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                    <!-- 报警日志 -->
                    <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="10" Margin="2,0,0,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <TextBlock Text="报警日志" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding AlarmLogs}"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,1"><Grid.ColumnDefinitions><ColumnDefinition Width="50"/><ColumnDefinition Width="70"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Time}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Code}" Foreground="{StaticResource ErrorBrush}" FontSize="9" FontWeight="Bold"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Message}" Foreground="{StaticResource ErrorBrush}" FontSize="9"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <!-- ═══ 右侧工具栏：JOG / 气缸 / IO / 扫码 ═══ -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="4,0,0,0">

                    <!-- JOG 手动轴控制 -->
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="10" Margin="0,0,0,4">
                        <Expander Header="🔧 手动轴控制 (JOG)" IsExpanded="{Binding IsJogExpanded}" Foreground="{StaticResource TextBrush}" FontSize="12">
                            <StackPanel Margin="0,6,0,0">
                                <ComboBox ItemsSource="{Binding AxisNames}" SelectedIndex="{Binding SelectedAxis}" Background="{StaticResource InputBrush}" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <TextBlock Text="目标:" Foreground="{StaticResource TextSecondaryBrush}" Width="40" VerticalAlignment="Center"/>
                                    <TextBox Text="{Binding JogTarget, StringFormat={}{0:F3}}" Width="70" Background="{StaticResource InputBrush}" Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Text="mm" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <TextBlock Text="速度:" Foreground="{StaticResource TextSecondaryBrush}" Width="40" VerticalAlignment="Center"/>
                                    <TextBox Text="{Binding JogSpeed}" Width="70" Background="{StaticResource InputBrush}" Foreground="{StaticResource TextBrush}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <Button Content="绝对" Width="50" Height="24" FontSize="11" Background="{StaticResource RunBrush}" Margin="0,0,4,0" Command="{Binding JogAbsCommand}"/>
                                    <Button Content="相对" Width="50" Height="24" FontSize="11" Background="{StaticResource WarnBrush}" Margin="0,0,4,0" Command="{Binding JogRelCommand}"/>
                                    <Button Content="停止" Width="50" Height="24" FontSize="11" Background="{StaticResource ErrorBrush}" Command="{Binding JogStopCommand}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding CurrentPos, StringFormat=当前位置: {0:F3} mm}" Foreground="{StaticResource AccentBrush}" FontSize="11" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Expander>
                    </Border>

                    <!-- 气缸手动覆写 -->
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="10" Margin="0,0,0,4">
                        <Expander Header="🔩 气缸手动控制" IsExpanded="{Binding IsCylinderExpanded}" Foreground="{StaticResource TextBrush}" FontSize="12">
                            <ItemsControl ItemsSource="{Binding Cylinders}" Margin="0,6,0,0">
                                <ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" FontSize="10" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="开" Width="32" Height="18" FontSize="9" Background="{StaticResource RunBrush}" Margin="0,0,3,0" Command="{Binding OnCommand}"/>
                                        <Button Content="关" Width="32" Height="18" FontSize="9" Background="{StaticResource ErrorBrush}" Command="{Binding OffCommand}"/>
                                    </StackPanel></Grid></DataTemplate></ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </Border>

                    <!-- IO 信号监视 -->
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="10" Margin="0,0,0,4">
                        <Expander Header="📶 IO 信号监视" IsExpanded="{Binding IsIoExpanded}" Foreground="{StaticResource TextBrush}" FontSize="12">
                            <StackPanel Margin="0,6,0,0">
                                <TextBlock Text="DI (输入)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" Margin="0,0,0,4"/>
                                <ItemsControl ItemsSource="{Binding DISignals}">
                                    <ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,1"><Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Index, StringFormat=DI{0}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9"/>
                                        <Ellipse Grid.Column="1" Width="8" Height="8" HorizontalAlignment="Left"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="Gray"/><Style.Triggers><DataTrigger Binding="{Binding Value}" Value="True"><Setter Property="Fill" Value="Lime"/></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse></Grid></DataTemplate></ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="DO (输出)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" Margin="0,8,0,4"/>
                                <ItemsControl ItemsSource="{Binding DOSignals}">
                                    <ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,1"><Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Index, StringFormat=DO{0}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9"/>
                                        <Ellipse Grid.Column="1" Width="8" Height="8" HorizontalAlignment="Left"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="Gray"/><Style.Triggers><DataTrigger Binding="{Binding Value}" Value="True"><Setter Property="Fill" Value="Orange"/></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse></Grid></DataTemplate></ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Expander>
                    </Border>

                    <!-- 扫码记录 -->
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="10">
                        <Expander Header="📷 扫码记录" IsExpanded="{Binding IsScanExpanded}" Foreground="{StaticResource TextBrush}" FontSize="12">
                            <StackPanel Margin="0,6,0,0">
                                <TextBlock Text="底板扫码:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9"/>
                                <ListBox ItemsSource="{Binding ScanLogBottom}" Height="60" Background="{StaticResource SurfaceDarkBrush}" Foreground="{StaticResource AccentBrush}" FontSize="9" BorderThickness="0"/>
                                <TextBlock Text="片源扫码:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9" Margin="0,4,0,0"/>
                                <ListBox ItemsSource="{Binding ScanLogSlice}" Height="60" Background="{StaticResource SurfaceDarkBrush}" Foreground="Lime" FontSize="9" BorderThickness="0"/>
                            </StackPanel>
                        </Expander>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- ═══ 底栏 ═══ -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="4" Margin="0,4,0,0">
            <Grid Margin="12,0">
                <TextBlock Text="{Binding WorkInfo}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Text="{Binding AlarmCode}" Foreground="{StaticResource ErrorBrush}" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock Text=" " Foreground="{StaticResource ErrorBrush}" FontSize="11"/>
                    <TextBlock Text="{Binding AlarmMessage}" Foreground="{StaticResource ErrorBrush}" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
