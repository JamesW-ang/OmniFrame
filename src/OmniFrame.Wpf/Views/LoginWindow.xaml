<Window x:Class="OmniFrame.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OmniFrame — 登录" Height="320" Width="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="OmniFrame" FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" Margin="0,10,0,20"/>

            <TextBlock Grid.Row="1" Text="用户名" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <TextBox Grid.Row="2" x:Name="txtUserId"
                     Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}"
                     Height="36" FontSize="14" Margin="0,0,0,12"
                     Background="{StaticResource InputBrush}" Foreground="{StaticResource TextBrush}"
                     BorderThickness="0"/>

            <TextBlock Grid.Row="3" Text="密码" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <PasswordBox Grid.Row="4" x:Name="txtPassword"
                         Height="36" FontSize="14" Margin="0,0,0,12"
                         Background="{StaticResource InputBrush}" Foreground="{StaticResource TextBrush}"
                         BorderThickness="0"/>

            <StackPanel Grid.Row="5" Margin="0,8,0,0">
                <Button Content="登 录" Height="40" FontSize="16"
                        Background="{StaticResource AccentBrush}" Foreground="White"
                        BorderThickness="0" Cursor="Hand"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=txtPassword}"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}"
                           HorizontalAlignment="Center" Margin="0,8,0,0" FontSize="12"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
