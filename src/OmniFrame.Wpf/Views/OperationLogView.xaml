<UserControl x:Class="OmniFrame.Wpf.Views.OperationLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataGrid ItemsSource="{Binding Entries}" AutoGenerateColumns="True" Margin="12"
              Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}"
              RowBackground="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"/>
</UserControl>
